# <Feature title> — Tasks

> Atomic, sequential, independent steps. The Builder works these top to bottom,
> one at a time. Each task names the R-id(s) it satisfies. Check off when done.

- [ ] **T1** (R1) — <single concrete step, e.g. "add `handoffRequested` field to Conversation model in `models/conversation.py`">
- [ ] **T2** (R1, R2) — <step>
- [ ] **T3** (R3) — <step>
- [ ] **T4** (R4) — <step>
- [ ] **T5** — Write tests per `<feature>.tests.md`
- [ ] **T6** — Run `./init.sh` + test command; ensure green before hand-off
