# Vision — <project / north star title>

> The whole-project north star a `/sdd-plan` session produces. This is the document
> the roadmap of `draft` epics rolls up to. It **complements** — it does **not**
> supersede or absorb — `specs/product.md` (the Layer-0 constitution: audience,
> domain model, principles) and `specs/glossary.md` (the term index). Reference
> those rather than duplicating them; `/sdd-plan` never rewrites or deletes them.

## Problem
<The core problem this project exists to solve, and why it matters now.>

## Users / audience
<Who this serves — the primary users and stakeholders. Cross-reference the
audience/domain model in `specs/product.md` instead of restating it.>

## Outcomes
<What success looks like at the whole-project level — the durable outcomes, not
the features that deliver them.>

## Non-goals
<What this project deliberately will NOT do. The boundaries that keep the roadmap
honest.>
